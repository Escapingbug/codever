packages:
  - apps/*
  - packages/*
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
  workerd: true

# Optional session extensions under extensions/ are intentionally not product workspaces.
